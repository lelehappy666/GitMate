# 分支标签与议题实验功能开关实施计划

> **执行要求：** 必须使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans` 按任务逐项实施。每个步骤使用复选框跟踪。

**目标：** 增加一个默认关闭、可持久化的“分支、标签与议题管理”实验功能开关，关闭时隐藏入口并禁止创建相关运行时。

**架构：** 在 `GitMateCore` 中增加可注入的偏好存储、可观察界面模型和纯状态访问策略；在 `GitMateApp` 根视图中共享该状态，并由根视图统一呈现设置弹窗。全局侧边栏只负责根据状态显示入口，根视图负责打开前校验与关闭后清理。

**技术栈：** Swift 6、SwiftUI、Observation、UserDefaults、Swift Package Manager、自定义 `GitMateCoreTestsRunner`。

## 全局约束

- 支持系统保持 macOS 14.0 及以上。
- 不引入新的第三方依赖。
- 开关默认值必须是 `false`，并且对所有账户和仓库全局生效。
- 关闭时不构建 `RepositoryWorkspaceRuntime`，不发起隐藏功能的网络请求。
- 不删除或改变 GitHub 上的远程分支、标签、规则、议题、里程碑和议题标签。
- 提交图的本地 Git 只读分支关系、分支筛选和提交拓扑必须保持不变。
- 所有文档和 Git 提交信息使用中文。
- 所有实施必须保留在 `feature/分支标签与议题` 分支。

---

## 文件结构

### 新增文件

- `Sources/GitMateCore/Preferences/ExperimentalFeaturePreferences.swift`：定义实验功能存储协议、`UserDefaults` 实现、内存实现、可观察偏好模型和管理工作区访问策略。
- `Tests/GitMateCoreTests/ExperimentalFeaturePreferencesTests.swift`：验证默认值、持久化、内存隔离、可观察模型与访问策略。
- `Sources/GitMateApp/Settings/GitMateSettingsView.swift`：呈现“实验功能”设置弹窗。

### 修改文件

- `Tests/GitMateCoreTests/TestMain.swift`：注册新测试集。
- `Sources/GitMateApp/GitMateApp.swift`：创建并注入全局实验功能偏好模型。
- `Sources/GitMateApp/Workspace/GitMateRootView.swift`：管理设置弹窗、打开安全门和关闭后的运行时清理。
- `Sources/GitMateApp/Workspace/WorkspaceRootView.swift`：向全局侧边栏传递开关状态与设置回调。
- `Sources/GitMateApp/Workspace/WorkspaceSidebar.swift`：隐藏或显示仓库管理入口，并将底部设置改为按钮。
- `Sources/GitMateApp/Workspace/RepositoryWorkspaceRootView.swift`：向仓库管理侧边栏传递设置回调。
- `Sources/GitMateApp/Workspace/RepositorySidebarView.swift`：将“仓库设置”改为打开同一设置弹窗的按钮。
- `README.md`：说明仓库管理功能默认关闭及重新开启路径。
- `docs/development/分支标签与议题验证清单.md`：增加开关界面和安全门验收项。

---

### Task 1（任务 1）：实验功能偏好与访问策略

**文件：**

- 新增：`Sources/GitMateCore/Preferences/ExperimentalFeaturePreferences.swift`
- 新增：`Tests/GitMateCoreTests/ExperimentalFeaturePreferencesTests.swift`
- 修改：`Tests/GitMateCoreTests/TestMain.swift`

**接口：**

- 输入：`UserDefaults` 或内存布尔值存储。
- 输出：`ExperimentalFeaturePreferenceStoring`、`UserDefaultsExperimentalFeaturePreferenceStore`、`InMemoryExperimentalFeaturePreferenceStore`、`ExperimentalFeaturePreferences`、`RepositoryManagementAccessPolicy`。
- `ExperimentalFeaturePreferences.repositoryManagementEnabled: Bool` 是后续视图唯一使用的开关真值。

- [ ] **步骤 1：编写偏好存储与访问策略的失败测试**

在新测试文件中定义以下测试集，并将其注册到 `TestMain.swift`：

```swift
import Foundation
import GitMateCore

let experimentalFeaturePreferenceTests = [
    TestCase("实验功能默认关闭且持久化开关值") {
        let suiteName = "GitMateExperimentalFeatures-\(UUID().uuidString)"
        let defaults = UserDefaults(suiteName: suiteName)!
        defer { defaults.removePersistentDomain(forName: suiteName) }
        let store = UserDefaultsExperimentalFeaturePreferenceStore(
            defaults: defaults
        )

        try expect(
            store.repositoryManagementEnabled() == false,
            "未保存偏好时必须默认关闭"
        )
        store.setRepositoryManagementEnabled(true)
        try expect(
            store.repositoryManagementEnabled(),
            "开启状态必须可以读回"
        )
        store.setRepositoryManagementEnabled(false)
        try expect(
            store.repositoryManagementEnabled() == false,
            "关闭状态必须可以覆盖保存"
        )
    },
    TestCase("实验功能内存存储实例相互隔离") {
        let first = InMemoryExperimentalFeaturePreferenceStore()
        let second = InMemoryExperimentalFeaturePreferenceStore()
        first.setRepositoryManagementEnabled(true)

        try expect(first.repositoryManagementEnabled(), "第一个存储应开启")
        try expect(
            second.repositoryManagementEnabled() == false,
            "第二个存储不得被污染"
        )
    },
    TestCase("管理工作区访问策略拒绝关闭状态") {
        let disabled = RepositoryManagementAccessPolicy(isEnabled: false)
        let enabled = RepositoryManagementAccessPolicy(isEnabled: true)

        try expect(disabled.canOpenWorkspace == false, "关闭时不得打开")
        try expect(
            disabled.shouldDismissWorkspace(isPresented: true),
            "关闭时必须清理已展示的工作区"
        )
        try expect(enabled.canOpenWorkspace, "开启时应允许打开")
        try expect(
            enabled.shouldDismissWorkspace(isPresented: true) == false,
            "开启时不得误关闭工作区"
        )
    }
]
```

- [ ] **步骤 2：运行测试并确认因类型尚未定义而失败**

运行：

```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
CLANG_MODULE_CACHE_PATH=/private/tmp/gitmate-clang-module-cache \
SWIFTPM_MODULECACHE_OVERRIDE=/private/tmp/gitmate-swiftpm-module-cache \
xcrun swift run GitMateCoreTestsRunner
```

预期：编译失败，并提示 `UserDefaultsExperimentalFeaturePreferenceStore`、`InMemoryExperimentalFeaturePreferenceStore` 或 `RepositoryManagementAccessPolicy` 未定义。

- [ ] **步骤 3：实现存储、可观察模型与访问策略**

新文件使用以下公开接口：

```swift
import Foundation
import Observation

public protocol ExperimentalFeaturePreferenceStoring: Sendable {
    func repositoryManagementEnabled() -> Bool
    func setRepositoryManagementEnabled(_ enabled: Bool)
}

public final class UserDefaultsExperimentalFeaturePreferenceStore:
    ExperimentalFeaturePreferenceStoring,
    @unchecked Sendable
{
    private let defaults: UserDefaults
    private let lock = NSLock()
    private let key = "GitMate.ExperimentalFeatures.RepositoryManagementEnabled"

    public init(defaults: UserDefaults = .standard) {
        self.defaults = defaults
    }

    public func repositoryManagementEnabled() -> Bool {
        lock.lock()
        defer { lock.unlock() }
        return defaults.bool(forKey: key)
    }

    public func setRepositoryManagementEnabled(_ enabled: Bool) {
        lock.lock()
        defer { lock.unlock() }
        defaults.set(enabled, forKey: key)
    }
}

public final class InMemoryExperimentalFeaturePreferenceStore:
    ExperimentalFeaturePreferenceStoring,
    @unchecked Sendable
{
    private let lock = NSLock()
    private var enabled: Bool

    public init(repositoryManagementEnabled: Bool = false) {
        enabled = repositoryManagementEnabled
    }

    public func repositoryManagementEnabled() -> Bool {
        lock.lock()
        defer { lock.unlock() }
        return enabled
    }

    public func setRepositoryManagementEnabled(_ enabled: Bool) {
        lock.lock()
        defer { lock.unlock() }
        self.enabled = enabled
    }
}

@MainActor
@Observable
public final class ExperimentalFeaturePreferences {
    public private(set) var repositoryManagementEnabled: Bool
    @ObservationIgnored private let store: any ExperimentalFeaturePreferenceStoring

    public init(store: any ExperimentalFeaturePreferenceStoring) {
        self.store = store
        repositoryManagementEnabled = store.repositoryManagementEnabled()
    }

    public func setRepositoryManagementEnabled(_ enabled: Bool) {
        guard repositoryManagementEnabled != enabled else { return }
        store.setRepositoryManagementEnabled(enabled)
        repositoryManagementEnabled = enabled
    }
}

public struct RepositoryManagementAccessPolicy: Equatable, Sendable {
    public let isEnabled: Bool

    public init(isEnabled: Bool) {
        self.isEnabled = isEnabled
    }

    public var canOpenWorkspace: Bool { isEnabled }

    public func shouldDismissWorkspace(isPresented: Bool) -> Bool {
        isPresented && !isEnabled
    }
}
```

- [ ] **步骤 4：补充可观察模型的写入测试**

在同一测试集增加：

```swift
TestCase("实验功能模型更新界面状态并保存") {
    let store = InMemoryExperimentalFeaturePreferenceStore()
    let model = await MainActor.run {
        ExperimentalFeaturePreferences(store: store)
    }

    await MainActor.run {
        model.setRepositoryManagementEnabled(true)
    }

    let value = await MainActor.run {
        model.repositoryManagementEnabled
    }
    try expect(value, "模型应立即更新界面状态")
    try expect(store.repositoryManagementEnabled(), "模型应同步保存偏好")
}
```

- [ ] **步骤 5：运行新测试并确认通过**

运行与步骤 2 相同的测试命令。

预期：新增的 4 个测试全部通过，其他已有测试不失败。

- [ ] **步骤 6：提交偏好存储与策略**

```bash
git add Sources/GitMateCore/Preferences/ExperimentalFeaturePreferences.swift \
  Tests/GitMateCoreTests/ExperimentalFeaturePreferencesTests.swift \
  Tests/GitMateCoreTests/TestMain.swift
git commit -m '增加实验功能偏好存储'
```

---

### Task 2（任务 2）：设置弹窗、入口显示与根视图安全门

**文件：**

- 新增：`Sources/GitMateApp/Settings/GitMateSettingsView.swift`
- 修改：`Sources/GitMateApp/GitMateApp.swift`
- 修改：`Sources/GitMateApp/Workspace/GitMateRootView.swift`
- 修改：`Sources/GitMateApp/Workspace/WorkspaceRootView.swift`
- 修改：`Sources/GitMateApp/Workspace/WorkspaceSidebar.swift`
- 修改：`Sources/GitMateApp/Workspace/RepositoryWorkspaceRootView.swift`
- 修改：`Sources/GitMateApp/Workspace/RepositorySidebarView.swift`

**接口：**

- 输入：任务 1 产生的 `ExperimentalFeaturePreferences.repositoryManagementEnabled`。
- 输出：`GitMateSettingsView`；`WorkspaceSidebar` 的 `repositoryManagementEnabled` 和 `onSettingsRequested`；`RepositorySidebarView` 的 `onSettingsRequested`。
- 根视图是唯一可以创建或清除 `RepositoryWorkspaceRuntime` 的组件。

- [ ] **步骤 1：新增设置弹窗视图**

创建 `GitMateSettingsView.swift`，使用下列结构：

```swift
import GitMateCore
import SwiftUI

struct GitMateSettingsView: View {
    @Bindable var preferences: ExperimentalFeaturePreferences
    let onClose: () -> Void

    var body: some View {
        VStack(alignment: .leading, spacing: 0) {
            HStack {
                VStack(alignment: .leading, spacing: 4) {
                    Text("设置")
                        .font(.system(size: 22, weight: .bold))
                    Text("管理 GitMate 的本机功能偏好")
                        .foregroundStyle(GitMateTheme.textSecondary)
                }
                Spacer()
                Button("完成", action: onClose)
                    .buttonStyle(.borderedProminent)
            }
            .padding(24)

            Divider()

            VStack(alignment: .leading, spacing: 14) {
                Text("实验功能")
                    .font(.system(size: 15, weight: .semibold))
                Toggle(
                    "分支、标签与议题管理",
                    isOn: Binding(
                        get: { preferences.repositoryManagementEnabled },
                        set: preferences.setRepositoryManagementEnabled
                    )
                )
                .accessibilityIdentifier(
                    "settings.experimental.repositoryManagement"
                )
                Text("启用后可以管理 GitHub 分支、标签、规则和议题。该功能仍处于实验阶段。")
                    .font(.system(size: 12))
                    .foregroundStyle(GitMateTheme.textSecondary)
            }
            .padding(24)
        }
        .frame(width: 520, height: 280)
        .background(.white)
    }
}
```

- [ ] **步骤 2：在应用入口创建全局偏好模型**

在 `GitMateApp` 增加：

```swift
@State private var experimentalFeatures: ExperimentalFeaturePreferences
```

在 `init()` 中分别初始化预览与生产偏好。预览分支使用内存存储：

```swift
if let page = Self.previewPage {
    _experimentalFeatures = State(
        initialValue: ExperimentalFeaturePreferences(
            store: InMemoryExperimentalFeaturePreferenceStore(
                repositoryManagementEnabled: (16...23).contains(page)
            )
        )
    )
    _viewModel = State(
        initialValue: OnboardingPreviewFactory.make(
            page: min(page, 9)
        )
    )
    workspaceRuntime = WorkspaceRuntimeDependencies(
        syncDestination: FileManager.default.temporaryDirectory,
        cacheDirectory: FileManager.default.temporaryDirectory,
        credentialStore: InMemoryCredentialStore()
    )
    repositoryWorkspaceFactory = nil
    let usesRepositoryWorkspace = page == 12
        || page == 13
        || (16...23).contains(page)
    repositoryWorkspacePreview = usesRepositoryWorkspace
        ? RepositoryWorkspacePreviewFactory.make(
            page: page,
            state: Self.previewState
        )
        : nil
    workspacePreview = page >= 10 && !usesRepositoryWorkspace
        ? WorkspacePreviewFactory.make(
            page: page,
            state: Self.previewState
        )
        : nil
    return
}
```

生产分支使用 `UserDefaults` 存储：

```swift
_experimentalFeatures = State(
    initialValue: ExperimentalFeaturePreferences(
        store: UserDefaultsExperimentalFeaturePreferenceStore()
    )
)
```

将 `experimentalFeatures` 传入 `GitMateRootView`。直接页面预览仍继续走现有预览工厂，并且只使用内存开关，不读取或修改真实开关。

- [ ] **步骤 3：由根视图统一呈现设置并执行安全门**

在 `GitMateRootView` 增加：

```swift
@Bindable var experimentalFeatures: ExperimentalFeaturePreferences
@State private var isSettingsPresented = false

private var repositoryManagementAccess: RepositoryManagementAccessPolicy {
    RepositoryManagementAccessPolicy(
        isEnabled: experimentalFeatures.repositoryManagementEnabled
    )
}
```

为根层 `Group` 增加：

```swift
.sheet(isPresented: $isSettingsPresented) {
    GitMateSettingsView(
        preferences: experimentalFeatures,
        onClose: { isSettingsPresented = false }
    )
}
.onChange(of: experimentalFeatures.repositoryManagementEnabled) {
    _, _ in
    if repositoryManagementAccess.shouldDismissWorkspace(
        isPresented: repositoryWorkspace != nil
    ) {
        repositoryWorkspace = nil
    }
}
```

在 `openRepositoryTools` 开头增加：

```swift
guard repositoryManagementAccess.canOpenWorkspace else { return }
```

分别向 `WorkspaceRootView` 和 `RepositoryWorkspaceRootView` 传入 `{ isSettingsPresented = true }`，并向 `WorkspaceRootView` 传入当前布尔值。

- [ ] **步骤 4：在全局工作区隐藏入口并启用设置按钮**

为 `WorkspaceRootView` 增加具有默认值的入口，保证已有预览工厂可以继续编译：

```swift
let repositoryManagementEnabled: Bool
let onSettingsRequested: () -> Void
```

初始化器默认值：

```swift
repositoryManagementEnabled: Bool = false,
onSettingsRequested: @escaping () -> Void = {}
```

将它们传入 `WorkspaceSidebar`。在 `WorkspaceSidebar` 中用显式条件构建入口：

```swift
if repositoryManagementEnabled {
    actionButton(
        title: "分支、标签与议题",
        symbol: "arrow.triangle.branch"
    ) {
        guard let currentRepositoryID else { return }
        onOpenRepositoryTools(currentRepositoryID)
    }
    .accessibilityIdentifier(
        "workspace.sidebar.repositoryManagement"
    )
}
```

将静态 `Label("设置", systemImage: "gearshape")` 替换为保持原有间距的无边框按钮，点击调用 `onSettingsRequested`，并添加可访问标识 `workspace.sidebar.settings`。

- [ ] **步骤 5：让仓库管理工作区可以打开同一设置**

为 `RepositoryWorkspaceRootView` 和 `RepositorySidebarView` 增加：

```swift
var onSettingsRequested: (() -> Void)? = nil
```

将 `RepositorySidebarView.settingsButton` 改为 `Button`；有回调时启用，无回调的独立预览中保持静态外观。按钮添加可访问标识 `repositoryWorkspace.sidebar.settings`。

- [ ] **步骤 6：编译应用并修正所有初始化器调用**

运行：

```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
CLANG_MODULE_CACHE_PATH=/private/tmp/gitmate-clang-module-cache \
SWIFTPM_MODULECACHE_OVERRIDE=/private/tmp/gitmate-swiftpm-module-cache \
xcrun swift build
```

预期：`Build complete!`，并且没有未处理的初始化器参数或 Swift 6 并发错误。

- [ ] **步骤 7：运行完整核心测试**

运行：

```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
CLANG_MODULE_CACHE_PATH=/private/tmp/gitmate-clang-module-cache \
SWIFTPM_MODULECACHE_OVERRIDE=/private/tmp/gitmate-swiftpm-module-cache \
xcrun swift run GitMateCoreTestsRunner
```

预期：原有 570 个测试和任务 1 新增的 4 个测试全部通过，失败数为 0。

- [ ] **步骤 8：提交设置界面与入口安全门**

```bash
git add Sources/GitMateApp/Settings/GitMateSettingsView.swift \
  Sources/GitMateApp/GitMateApp.swift \
  Sources/GitMateApp/Workspace/GitMateRootView.swift \
  Sources/GitMateApp/Workspace/WorkspaceRootView.swift \
  Sources/GitMateApp/Workspace/WorkspaceSidebar.swift \
  Sources/GitMateApp/Workspace/RepositoryWorkspaceRootView.swift \
  Sources/GitMateApp/Workspace/RepositorySidebarView.swift
git commit -m '接入分支标签与议题实验开关'
```

---

### Task 3（任务 3）：文档、界面验收与发布包

**文件：**

- 修改：`README.md`
- 修改：`docs/development/分支标签与议题验证清单.md`
- 生成：`dist/GitMate.app`（已忽略）
- 生成：`dist/GitMate-macOS-arm64.zip`（已忽略）

**接口：**

- 输入：任务 2 产生的设置弹窗和入口安全门。
- 输出：中文使用说明、界面验收记录、经过验证的 macOS 测试应用和压缩包。

- [ ] **步骤 1：更新使用说明与验证清单**

在 `README.md` 的仓库管理说明中明确写入：

```markdown
> 分支、标签与议题管理默认作为实验功能关闭。可在“设置 → 实验功能”中开启，提交图的分支关系不受影响。
```

在验证清单顶部增加一组“实验功能开关”步骤：默认隐藏、开启立即显示、使用中关闭返回全局工作区、重启后状态保留、提交图不受影响。

- [ ] **步骤 2：执行代码与文档静态检查**

运行：

```bash
git diff --check
```

预期：`git diff --check` 无输出；逐段阅读后确认文档每项验收要求都具有明确操作与预期结果。

- [ ] **步骤 3：执行最终完整构建与测试**

运行：

```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
CLANG_MODULE_CACHE_PATH=/private/tmp/gitmate-clang-module-cache \
SWIFTPM_MODULECACHE_OVERRIDE=/private/tmp/gitmate-swiftpm-module-cache \
xcrun swift build

DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
CLANG_MODULE_CACHE_PATH=/private/tmp/gitmate-clang-module-cache \
SWIFTPM_MODULECACHE_OVERRIDE=/private/tmp/gitmate-swiftpm-module-cache \
xcrun swift run GitMateCoreTestsRunner
```

预期：构建成功，所有测试通过，失败数为 0。

- [ ] **步骤 4：手动验收开关界面与回退行为**

使用真实应用会话执行：

1. 打开工作区，确认默认不存在“分支、标签与议题”入口。
2. 点击侧边栏底部“设置”，开启实验功能，确认入口原位出现。
3. 进入仓库管理工作区，点击“仓库设置”关闭开关，确认立即返回全局工作区。
4. 重启应用，确认开关保持关闭。
5. 打开提交图，确认分支列表、分支连线和筛选仍正常。

- [ ] **步骤 5：提交文档与验收清单**

```bash
git add README.md docs/development/分支标签与议题验证清单.md
git commit -m '补充实验功能开关验证说明'
```

- [ ] **步骤 6：构建发布版并生成本地签名测试包**

运行发布构建：

```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
CLANG_MODULE_CACHE_PATH=/private/tmp/gitmate-clang-module-cache \
SWIFTPM_MODULECACHE_OVERRIDE=/private/tmp/gitmate-swiftpm-module-cache \
xcrun swift build -c release
```

使用临时目录组装 `GitMate.app`，复用已验证的 `Info.plist`，将 `.build/arm64-apple-macosx/release/GitMate` 放入 `Contents/MacOS/GitMate`，然后执行：

```bash
GITMATE_PACKAGE_DIR=$(mktemp -d /private/tmp/gitmate-feature-package.XXXXXX)
mkdir -p "$GITMATE_PACKAGE_DIR/GitMate.app/Contents/MacOS" \
  "$GITMATE_PACKAGE_DIR/GitMate.app/Contents/Resources"
cp dist/GitMate.app/Contents/Info.plist \
  "$GITMATE_PACKAGE_DIR/GitMate.app/Contents/Info.plist"
cp .build/arm64-apple-macosx/release/GitMate \
  "$GITMATE_PACKAGE_DIR/GitMate.app/Contents/MacOS/GitMate"
codesign --force --deep --sign - "$GITMATE_PACKAGE_DIR/GitMate.app"
ditto -c -k --sequesterRsrc --keepParent \
  "$GITMATE_PACKAGE_DIR/GitMate.app" \
  "$GITMATE_PACKAGE_DIR/GitMate-macOS-arm64.zip"
```

用新产物替换 `dist/GitMate.app` 和 `dist/GitMate-macOS-arm64.zip`，旧产物先移入本次临时目录，不直接删除。

- [ ] **步骤 7：验证发布包完整性**

运行：

```bash
plutil -lint dist/GitMate.app/Contents/Info.plist
codesign --verify --deep --strict --verbose=2 dist/GitMate.app
unzip -t dist/GitMate-macOS-arm64.zip
shasum -a 256 \
  dist/GitMate.app/Contents/MacOS/GitMate \
  dist/GitMate-macOS-arm64.zip
```

预期：`Info.plist` 正确，代码签名有效，压缩包没有错误，并记录两个产物的 SHA-256。

---

## 完成标准

- 新用户和升级用户都默认看不到“分支、标签与议题”入口。
- 设置弹窗可以即时开启或关闭功能，并在重启后保留值。
- 关闭时无法通过界面或根视图调用打开管理工作区。
- 在仓库管理工作区中关闭功能会立即安全返回全局工作区。
- 提交图的分支关系与筛选不受影响。
- 完整构建成功，所有自动测试失败数为 0。
- `dist/GitMate.app` 和 `dist/GitMate-macOS-arm64.zip` 已更新并通过完整性校验。
